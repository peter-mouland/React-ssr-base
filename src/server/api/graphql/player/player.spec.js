import { expect, sinon } from '../../../../../tests/config/test.helper';

const sanbox = sinon.sandbox.create();

describe('player', ()=>{

  beforeEach(()=>{

  });

  afterEach(()=>{
    sanbox.restore();
  });

});
